# Fichiers IntelliJ / JetBrains
.idea/          ← ignore les fichiers de configuration d'IntelliJ
*.iml           ← ignore les fichiers `.iml` des projets IntelliJ

# Dossier de sortie
/out/           ← ignore le dossier de compilation (souvent utilisé dans les projets Java)
target/         ← ignore le dossier de sortie pour Maven (Java aussi)

# Fichiers système
.DS_Store       ← fichier macOS (inutile dans un repo)
Thumbs.db       ← fichier Windows (inutile aussi)
